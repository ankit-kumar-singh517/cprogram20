#include<stdio.h>  //preprocessor directive 
int main()           //if we use "void" than we dont use return 0;
{
   //input->process(logic)->output  //priviple of computer
   int num=10;
   printf("hello i m using c commamnd ");
   scanf("%d",&num);
   printf("your no.=%d",num);
   return 0;
}
//function is block of code ,which have name and can be execute again and again,
//it may be possible to pass argument to the function and it can return value if required